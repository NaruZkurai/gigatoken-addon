# Rust build artifacts
/target
/gigatoken-target

# gigatoken patched source (regenerated by build.sh)
/gigatoken-src

# Cargo lock is committed for a library, but this is a standalone package;
# keep it for reproducible builds.
